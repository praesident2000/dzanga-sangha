{% extends "base.twig" %}

{% block content %}
    {% set meta = post.get_field('meta') %}
    {% set kickertxt %}
        {% if post.parent %} {{ post.parent.title }} {% else %} {{ post.title }} {% endif %}
    {% endset %}
    {# TO-DO: add page header #}
    <article class="post-type-{{ post.post_type }}" id="post-{{ post.ID }}">
        {% include "partial/page_header.twig" %}

        {% include "layouts/layouts.twig" %}

        {% include "partial/latest_news.twig" %}
        
	    {% include "partial/related_stories.twig" %}
    </article>
{% endblock %}